"""
Configure the tests.

Used by both the `pytest` and `unittest` libraries.

What to include here:
- Define custom fixtures, marks, etc. that should be globally accessible in "tests".
- Define custom fixtures with the `session` scope.

"""